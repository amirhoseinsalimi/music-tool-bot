name: music-tool-bot

services:
  bot:
    image: music-tool-bot:prod
    build:
      context: .
      dockerfile: docker/Dockerfile
    environment:
      - PTB_TIMEDELTA=true
    volumes:
      - bot_data:/data
      - ./.env:/app/.env:ro
    restart: unless-stopped

volumes:
  bot_data:
